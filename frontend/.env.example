# Backend API base URL used by the Vite client.
# Same machine: http://localhost:5000/api
# Another laptop on Wi-Fi: http://HOST_LAN_IP:5000/api
# If unset, the client uses the current page hostname and port 5000.
VITE_API_BASE_URL=http://localhost:5000/api
